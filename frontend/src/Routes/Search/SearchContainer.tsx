import React from "react";
import SearchPresenter from "./SearchPresenter";

class SearchContainer extends React.Component<any> {
  public render() {
    return <SearchPresenter />;
  }
}

export default SearchContainer;
