import React from "react";
import styled from "styled-components";

const Container = styled.div``;

const MatchPresenter: React.SFC = ({}) => {
  return <Container>hi</Container>;
};

export default MatchPresenter;
